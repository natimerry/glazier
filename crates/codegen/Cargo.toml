[package]
name = "glazier-codegen"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bindgen = "0.70"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing"] }

[target.'cfg(windows)'.dependencies]
winreg = "0.55.0"
