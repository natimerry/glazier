name: Build & Test

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    env:
      RUST_BACKTRACE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Verify submodules
        run: git submodule status

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: Install LLVM and MinGW
        run: choco install llvm mingw -y

      - name: Set LIBCLANG_PATH and add MinGW to PATH
        shell: pwsh
        run: |
          echo "LIBCLANG_PATH=C:\Program Files\LLVM\bin" >> $env:GITHUB_ENV
          echo "C:\ProgramData\mingw64\mingw64\bin" >> $env:GITHUB_PATH

      - name: Verify tools
        shell: pwsh
        run: |
          clang --version
          gcc --version

      - name: Compile C test sample with gcc
        shell: pwsh
        run: |
          gcc tests/samples/app_custom_section_with_imports.c `
            -o tests/samples/app_custom_section_with_imports.exe

      - name: Run cargo test
        shell: pwsh
        run: cargo test 2>&1 | Tee-Object -FilePath test_output.log

      - name: Upload test log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs
          path: test_output.log
